[project]
name = "backuprotation"
version = "0.0.2"
description = "A tool for rotating backups"
authors = [
    { name="bluePlatinum", email="jukic.rok@gmail.com" },
]
requires-python = ">=3.7"
readme = "README.md"
license = "MIT"
license-files = ["LICENSE.txt"]
dependencies = []

[project.scripts]
backuprotation = "backuprotation.main:main"

[project.optional-dependencies]
dev = ["pytest"]

[project.urls]
Homepage = "https://github.com/bluePlatinum/backuprotation"
